<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:LinguaSnapp.Views.ContentViews"
             x:Class="LinguaSnapp.Views.ContentViews.InfoButtonBar">
  <ContentView.Content>
        <Grid x:Name="MainGrid"
              ColumnDefinitions="*,*"
              HorizontalOptions="Center">
            <cv:IconLabelButton BindingContext="{Binding AccessButtonViewModel}" />
            <cv:IconLabelButton Grid.Column="1"
                                BindingContext="{Binding DataButtonViewModel}" />
        </Grid>
    </ContentView.Content>
</ContentView>